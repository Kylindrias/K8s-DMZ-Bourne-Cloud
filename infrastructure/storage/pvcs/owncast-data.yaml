apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owncast-data
  namespace: owncast
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi