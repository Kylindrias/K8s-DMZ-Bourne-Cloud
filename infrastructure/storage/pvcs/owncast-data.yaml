apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owncast-data
  namespace: owncast
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi