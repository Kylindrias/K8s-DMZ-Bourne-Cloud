apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: talos-3dv-weq             # must exactly match the k8s node name
  namespace: longhorn-system
spec:
  disks:
    xvdb-disk:
      path: /var/lib/longhorn/disks/xvdb
      allowScheduling: true
      storageReserved: 0